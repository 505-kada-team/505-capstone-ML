from app.main import app

# Vercel's Python runtime (@vercel/python) auto-detects an ASGI `app`
# object exported from this module and serves it directly -- no extra
# adapter (e.g. Mangum) is needed.
